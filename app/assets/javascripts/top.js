
entry_n = 0;

$(function() {

  // chapter = new ChapterView( { model: novel.chapters.models[0]} );
  isChapterHided = true;

  //init

  $("#picker").hide();

  $('img').dblclick(Picker.pickImage);

});
